# Development Environment Configuration
gateway.message=Hello World from DEV Environment!
gateway.environment=dev

# Server Configuration for Dev
server.port=8080

# Actuator Configuration (inherit from base)
management.endpoints.web.exposure.include=health,info,prometheus,metrics,env

# Additional Dev-specific settings
logging.level.com.example.demo=DEBUG
